<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>مولد أيقونات PWA - عيادة العامر</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #2E7D32, #1B5E20);
            color: white;
            text-align: center;
            padding: 20px;
        }
        .icon-container {
            display: inline-block;
            margin: 10px;
            padding: 20px;
            background: rgba(255,255,255,0.1);
            border-radius: 15px;
        }
        canvas {
            border: 2px solid rgba(255,255,255,0.3);
            border-radius: 10px;
            display: block;
            margin: 10px auto;
        }
        .download-btn {
            background: #4CAF50;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 25px;
            cursor: pointer;
            margin: 5px;
            font-size: 14px;
        }
        .download-btn:hover {
            background: #45a049;
        }
        h1 {
            margin-bottom: 30px;
        }
        .instructions {
            background: rgba(255,255,255,0.1);
            padding: 20px;
            border-radius: 15px;
            margin: 20px auto;
            max-width: 600px;
        }
    </style>
</head>
<body>
    <h1>🦷 مولد أيقونات PWA - عيادة العامر للأسنان</h1>
    
    <div class="instructions">
        <h3>تعليمات الاستخدام:</h3>
        <p>1. انقر على زر "تحميل" تحت كل أيقونة</p>
        <p>2. احفظ الملف في مجلد icons/ في مشروعك</p>
        <p>3. أعد تحميل PWABuilder وتأكد من حل المشاكل</p>
    </div>

    <div class="icon-container">
        <h3>أيقونة 48×48</h3>
        <canvas id="icon48" width="48" height="48"></canvas>
        <button class="download-btn" onclick="downloadIcon('icon48', 'icon-48x48.png')">تحميل</button>
    </div>

    <div class="icon-container">
        <h3>أيقونة 72×72</h3>
        <canvas id="icon72" width="72" height="72"></canvas>
        <button class="download-btn" onclick="downloadIcon('icon72', 'icon-72x72.png')">تحميل</button>
    </div>

    <div class="icon-container">
        <h3>أيقونة 96×96</h3>
        <canvas id="icon96" width="96" height="96"></canvas>
        <button class="download-btn" onclick="downloadIcon('icon96', 'icon-96x96.png')">تحميل</button>
    </div>

    <div class="icon-container">
        <h3>أيقونة 128×128</h3>
        <canvas id="icon128" width="128" height="128"></canvas>
        <button class="download-btn" onclick="downloadIcon('icon128', 'icon-128x128.png')">تحميل</button>
    </div>

    <div class="icon-container">
        <h3>أيقونة 144×144</h3>
        <canvas id="icon144" width="144" height="144"></canvas>
        <button class="download-btn" onclick="downloadIcon('icon144', 'icon-144x144.png')">تحميل</button>
    </div>

    <div class="icon-container">
        <h3>أيقونة 152×152</h3>
        <canvas id="icon152" width="152" height="152"></canvas>
        <button class="download-btn" onclick="downloadIcon('icon152', 'icon-152x152.png')">تحميل</button>
    </div>

    <div class="icon-container">
        <h3>أيقونة 192×192</h3>
        <canvas id="icon192" width="192" height="192"></canvas>
        <button class="download-btn" onclick="downloadIcon('icon192', 'icon-192x192.png')">تحميل</button>
    </div>

    <div class="icon-container">
        <h3>أيقونة 384×384</h3>
        <canvas id="icon384" width="384" height="384"></canvas>
        <button class="download-btn" onclick="downloadIcon('icon384', 'icon-384x384.png')">تحميل</button>
    </div>

    <div class="icon-container">
        <h3>أيقونة 512×512</h3>
        <canvas id="icon512" width="512" height="512"></canvas>
        <button class="download-btn" onclick="downloadIcon('icon512', 'icon-512x512.png')">تحميل</button>
    </div>

    <div class="icon-container">
        <h3>اختصار الحجز</h3>
        <canvas id="shortcut-booking" width="64" height="64"></canvas>
        <button class="download-btn" onclick="downloadIcon('shortcut-booking', 'shortcut-booking.png')">تحميل</button>
    </div>

    <div class="icon-container">
        <h3>اختصار لوحة التحكم</h3>
        <canvas id="shortcut-dashboard" width="64" height="64"></canvas>
        <button class="download-btn" onclick="downloadIcon('shortcut-dashboard', 'shortcut-dashboard.png')">تحميل</button>
    </div>

    <div class="icon-container">
        <h3>اختصار المبيعات</h3>
        <canvas id="shortcut-pos" width="64" height="64"></canvas>
        <button class="download-btn" onclick="downloadIcon('shortcut-pos', 'shortcut-pos.png')">تحميل</button>
    </div>

    <script>
        // Draw main app icon
        function drawMainIcon(canvasId) {
            const canvas = document.getElementById(canvasId);
            const ctx = canvas.getContext('2d');
            const size = canvas.width;
            
            // Background gradient
            const gradient = ctx.createLinearGradient(0, 0, size, size);
            gradient.addColorStop(0, '#2E7D32');
            gradient.addColorStop(1, '#1B5E20');
            
            // Rounded rectangle background
            ctx.fillStyle = gradient;
            const radius = size * 0.1;
            roundRect(ctx, radius, radius, size - 2*radius, size - 2*radius, radius);
            ctx.fill();
            
            // Add white border
            ctx.strokeStyle = '#FFFFFF';
            ctx.lineWidth = Math.max(2, size * 0.02);
            ctx.stroke();
            
            // Draw tooth emoji or dental symbol
            ctx.fillStyle = '#FFFFFF';
            ctx.font = `${size * 0.5}px Arial`;
            ctx.textAlign = 'center';
            ctx.textBaseline = 'middle';
            ctx.fillText('🦷', size / 2, size / 2);
        }
        
        // Draw shortcut icons
        function drawShortcutIcon(canvasId, emoji) {
            const canvas = document.getElementById(canvasId);
            const ctx = canvas.getContext('2d');
            const size = canvas.width;
            
            // Background gradient
            const gradient = ctx.createLinearGradient(0, 0, size, size);
            gradient.addColorStop(0, '#4CAF50');
            gradient.addColorStop(1, '#2E7D32');
            
            // Rounded rectangle background
            ctx.fillStyle = gradient;
            const radius = size * 0.15;
            roundRect(ctx, radius, radius, size - 2*radius, size - 2*radius, radius);
            ctx.fill();
            
            // Add white border
            ctx.strokeStyle = '#FFFFFF';
            ctx.lineWidth = Math.max(1, size * 0.03);
            ctx.stroke();
            
            // Draw emoji
            ctx.fillStyle = '#FFFFFF';
            ctx.font = `${size * 0.6}px Arial`;
            ctx.textAlign = 'center';
            ctx.textBaseline = 'middle';
            ctx.fillText(emoji, size / 2, size / 2);
        }
        
        // Helper function for rounded rectangles
        function roundRect(ctx, x, y, width, height, radius) {
            ctx.beginPath();
            ctx.moveTo(x + radius, y);
            ctx.lineTo(x + width - radius, y);
            ctx.quadraticCurveTo(x + width, y, x + width, y + radius);
            ctx.lineTo(x + width, y + height - radius);
            ctx.quadraticCurveTo(x + width, y + height, x + width - radius, y + height);
            ctx.lineTo(x + radius, y + height);
            ctx.quadraticCurveTo(x, y + height, x, y + height - radius);
            ctx.lineTo(x, y + radius);
            ctx.quadraticCurveTo(x, y, x + radius, y);
            ctx.closePath();
        }
        
        // Download function
        function downloadIcon(canvasId, filename) {
            const canvas = document.getElementById(canvasId);
            const link = document.createElement('a');
            link.download = filename;
            link.href = canvas.toDataURL('image/png');
            link.click();
        }
        
        // Generate all icons on page load
        window.onload = function() {
            // Main app icons
            drawMainIcon('icon48');
            drawMainIcon('icon72');
            drawMainIcon('icon96');
            drawMainIcon('icon128');
            drawMainIcon('icon144');
            drawMainIcon('icon152');
            drawMainIcon('icon192');
            drawMainIcon('icon384');
            drawMainIcon('icon512');
            
            // Shortcut icons
            drawShortcutIcon('shortcut-booking', '📅');
            drawShortcutIcon('shortcut-dashboard', '⚡');
            drawShortcutIcon('shortcut-pos', '💳');
        };
    </script>
</body>
</html>